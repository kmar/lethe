support routines written in the scripting language itself:

assert          = dumb assert
hash            = hash support routines
set             = hashset
map             = hashmap
priority_queue  = prioqueue using min-heap
rotate_array    = left rotate array/array view
queue           = double-ended queue

note that the license was modified to allow to distribute these scripts
without having to include the license itself in such case
